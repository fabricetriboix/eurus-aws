name: OpenTofu CI - env/common-nonprod

on:
  pull_request:
    types:
    - opened
    - reopened
    - synchronize
    branches:
    - main
    paths:
    - env/common-nonprod/**
    - .github/workflows/env-common-nonprod-ci.yaml
    - .github/actions/tg-ci/**
    - Makefile

permissions:
  id-token: write   # This is required for requesting the JWT
  contents: read    # This is required for actions/checkout
  security-events: write  # For `github/codeql-action/upload-sarif` to upload SARIF results

jobs:
  ci:
    runs-on: ubuntu-latest
    environment: common-nonprod

    steps:
    - name: Checkout the code
      uses: actions/checkout@v5

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v6
      with:
        role-to-assume: arn:aws:iam::${{ vars.AWS_MANAGEMENT_ACCOUNT_ID }}:role/github-actions-role
        role-session-name: github-actions
        aws-region: ${{ vars.AWS_REGION }}

    - name: STS get caller identity
      shell: bash
      run: |
        aws sts get-caller-identity

    - name: Run CI
      uses: ./.github/actions/tg-ci
      with:
        component-type: env
        component-name: common-nonprod
        role-arn: "arn:aws:iam::${{ vars.AWS_ACCOUNT_ID }}:role/tf-role-${{ vars.AWS_REGION }}"
