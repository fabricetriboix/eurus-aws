name: OpenTofu CI - feature/codeartifact

on:
  pull_request:
    types:
    - opened
    - reopened
    - synchronize
    branches:
    - main
    paths:
    - feature/codeartifact/**
    - .github/workflows/feature-codeartifact-ci.yaml
    - .github/actions/tf-ci/**
    - Makefile

permissions:
  contents: read
  security-events: write  # For `github/codeql-action/upload-sarif` to upload SARIF results

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout the code
      uses: actions/checkout@v5

    - name: Run CI
      uses: ./.github/actions/tf-ci
      with:
        component-type: feature
        component-name: codeartifact
